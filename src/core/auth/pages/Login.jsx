import React from "react";
import LoginForm from "@core/auth/components/login/LoginForm";

const Login = () => {
  // const {handleLogin, loading, error} = useLogin();
  return <LoginForm />;
};

export default Login;
